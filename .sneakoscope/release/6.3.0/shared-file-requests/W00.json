{
  "schema": "sks.shared-file-request.v1",
  "workstream": "W00",
  "reason": "The architecture budget SSOT must be present in packed installs because the compiled architecture check reads it at runtime.",
  "target_files": [
    "package.json"
  ],
  "required_change": "Add config/architecture-budgets.v1.json to package.json files without increasing the package size ceiling.",
  "depends_on_commit": null,
  "tests_requiring_change": [
    "npm pack --dry-run --ignore-scripts --json",
    "installed tarball architecture check smoke"
  ]
}
